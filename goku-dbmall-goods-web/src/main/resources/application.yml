server:
  port: 25000
spring:
  profiles:
    active: sony
  application:
    name: goku-dbmall-goods
  cloud:
    nacos:
      discovery:
        register-enabled: true
        server-addr: 192.168.50.167:8848
        weight: 1
        namespace: dev
management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  path: logs
  level:
    ROOT: info
    com.goku: debug